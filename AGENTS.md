# PC-alignment-Codex 作業規約

この規約は、どのPC・どのCodexセッションから作業しても守る。

1. 新しいPythonスクリプトをリポジトリ直下へ置かない。
2. ピラー1本ごとを主な単位にする解析は `pillar_level/vN_説明/` に置き、実行ファイル名を `pillar_` で始める。
3. 視野を主な単位にする集計・比較は `field_level/vN_説明/` に置き、実行ファイル名を `field_` で始める。
4. 両方が使う、粒度固有でない処理だけを `shared/` に置く。判断理由を `shared/README.md` に追記する。
5. 入力台帳は `data/raw/`、生成物は `data/results/` に置く。コードとデータを混在させない。実験画像本体・生成画像・キャッシュはGitへ追加しない。
6. 既存バージョンのコード・パラメータを上書きしない。変更は新しい `vN_説明/` フォルダを作り、旧版を残す。
7. 各バージョンには `NOTES.md` を必ず置き、実装変更、結果と保存先、既知の問題を記載する。
8. 一次記録に欠陥の記載がないことを、欠陥なしの根拠にしない。自動候補は目視承認なしに解析へ使わない。

詳細と例は `docs/REPOSITORY_CONVENTIONS.md` を参照する。
