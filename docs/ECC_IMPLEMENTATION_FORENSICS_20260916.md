# 2026-09-01 ECC実装の捜索記録 — 2026-09-16

## 結論

2026-09-01にFOV 1-6で記録された、アフィン変換・多段ピラミッドによるECC実行経路は、
このPCで読み取り可能なローカル作業フォルダ、Google Drive同期フォルダ、およびアクセス可能な
GitHubリポジトリの履歴からは特定できなかった。

`batch_register_images.py` は発見したが、いずれも2026-07-04〜07-15の別系統である。
この実装は `cv2.MOTION_EUCLIDEAN`（回転+並進）であり拡大縮小を推定しない。
従って、当時の記録にある拡大縮小差（−0.41%）を出力できず、0.125→0.25→0.5→1.0の
ピラミッドも実装していない。これを2026-09-01の再現テストに使うことは不適切であり、
実行は行わなかった。

## 捜索範囲と結果

- `C:\Users\chuya\Documents`、`C:\Users\chuya\Desktop`、ユーザーホーム直下
  （`AppData`は除外）: `batch_register_images.py` はなし。関連候補は既知の独立診断や
  2026-09-09取得済みの作業コピーのみ。
- `G:\マイドライブ`: `Claude\セッション\register_images.py` を発見したが、2026-06-30更新の
  スクラッチ検出による回転+並進スクリプトであり、ECCも多段ピラミッドもない。
- `PC-alignment-anti` のローカル作業コピーとGit履歴: ECC由来の `batch_register_images.py` を発見。
  ICP+SVDコードは確認・実行・改変していない。
- GitHub `chuya-dell/PC-alignment-anti` と `chuya-dell/PC-alignment-Codex`: ブランチ一覧を照合済み。
- GitHub `chuya-dell/PC-alignment-claude`: 読み取り可能。全リモートブランチと履歴を確認したが、
  2026-09-01前後のコミットはなく、最新の位置合わせ関連ブランチも2026-07-22以前だった。

## 発見した `batch_register_images.py`

### Claude版

- GitHub: `chuya-dell/PC-alignment-claude`
- ref: `origin/claude/batch-image-registration-qe4cp7`
- commit: `26e943dc5b302182a1bee97a1ec7aa5b34b98b47`（2026-07-15 UTC）
- ファイル: `batch_register_images.py`
- blob: `08de65ded9293a09b65aeb628e63df0337855fff`

### Anti側に残ったコピー

- GitHub: `chuya-dell/PC-alignment-anti`
- ref: `origin/claude/batch-image-registration-qe4cp7`
- commit: `8732156ef512c2e2e57269a2dd8c2730ed026ed1`（2026-07-04 UTC）
- ファイル: `batch_register_images.py`
- blob: `818bf0ea046088059ae7d6f109aca46815c8ab80`

後者も同名の履歴版だが、対象の9月1日実装ではない。Anti側はこのファイルの存在確認だけに
使い、ICP+SVDの内容確認・実行・改変はしていない。

## 発見版の設定（再現対象ではない）

| 項目 | 確認内容 |
| --- | --- |
| 前処理 | TIFFをfloat32として使用。高域強調・ガウシアン前処理・画像縮小はなし。スクラッチ周辺を切り出す。 |
| 初期化 | Hanning窓付き `phaseCorrelate` による並進初期値。 |
| 変換モデル | `cv2.MOTION_EUCLIDEAN`。回転+並進のみで、拡大縮小・剪断は含まない。 |
| 段階的縮小 | なし。全解像度で1回のECC。 |
| 収束条件 | 最大200反復、EPS `1e-6`。ECC非収束時は失敗扱い又はスクラッチ並進へフォールバック。 |
| マスク | ECC自体に解析欠陥マスクは渡さない。共通有効領域のクロップと、傷帯を除くパッチ評価はある。 |

## 2026-09-01の記録と照合できない理由

求める実装は少なくとも以下を満たす必要がある。

1. アフィン変換で拡大縮小差を出力できること。
2. 0.25→0.5→1.0（検証時は0.125開始を追加）の明示的なピラミッドを持つこと。
3. FOV 1-6で、相関0.971、水平+1.05 px、垂直+3.61 px、回転−0.033度、
   拡大縮小差−0.41%を同じ定義で出力すること。

発見版は(1)(2)を満たさないため、数値が一致しないことは実装上確定している。
したがって、この版での「深い初期化が効くか」の検証はまだ開始していない。

## 次に必要な一次資料

- 2026-09-01に実行したスクリプト本体、または当時の作業フォルダのバックアップ。
- 実行コマンド/設定（前処理、変換モデル、ピラミッド倍率、最大反復、EPS、入力マスク）。
- FOV 1-6の当時の出力CSVまたはログ。変換の向きと、相関値の計算領域も固定する。

これらを受領後にのみ、同じ入力画像・設定で当時の値を再現し、その状態から0.125開始と
初期並進グリッドを追加する。
